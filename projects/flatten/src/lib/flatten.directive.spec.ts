import { SpreadDirective } from './flatten.directive';

describe('FlattenDirective', () => {
  it('should create an instance', () => {
    const directive = new SpreadDirective();
    expect(directive).toBeTruthy();
  });
});
