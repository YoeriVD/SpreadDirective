import { BindingContextDirective } from './binding-context.directive';

describe('BindingContextDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingContextDirective();
    expect(directive).toBeTruthy();
  });
});
